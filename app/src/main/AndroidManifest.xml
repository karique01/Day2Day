<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.karique.work.dev.day2day">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ResetPasswordInAppActivity"></activity>
        <activity android:name=".activities.RegisterPacienteActivity" />
        <activity android:name=".activities.RegisterTutorActivity" />
        <activity android:name=".activities.AgregarDiarioActivity" />
        <activity android:name=".activities.RealizarTestActivity" />
        <activity android:name=".activities.MainTutorActivity" />
        <activity android:name=".activities.MainPacienteActivity" />
        <activity android:name=".activities.LoginTutorActivity" />
        <activity android:name=".activities.LoginPacienteActivity" />
        <activity android:name=".activities.ResetPasswordActivity" />
        <activity android:name=".activities.ChooseRegisterActivity" />
        <activity android:name=".activities.ChooseLoginActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_face" />

        <service android:name=".service.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>